# Phase 0 — Initial Build [DONE]

Step-by-step plan to implement the Commute Tracker bot from the current skeleton to a fully working application. Each phase builds on the previous one — complete and test each phase before moving to the next.

---

## Phase 1 — Project Bootstrap (DONE)

> Goal: compilable project with config loading, DB connection, and running migrations.

### 1.1 Initialize Go module
- [x] `go mod init github.com/yurii-merker/commute-tracker`
- [x] Add initial dependencies (`telebot.v4`, `pgx/v5`, `redis/v9`, `golang-migrate`, `sqlc`, `go.uber.org/mock`, `env/v11`)

### 1.2 Configuration (`internal/config/`)
- [x] Define `Config` struct with fields: `TelegramBotToken`, `NationalRailToken`, `DatabaseURL`, `RedisURL`, `LogLevel`
- [x] Load from environment variables using `caarlos0/env/v11`
- [x] Validate required fields on startup

### 1.3 Database connection (`internal/db/`)
- [x] Implement `Connect(ctx, databaseURL) (*pgxpool.Pool, error)`
- [x] Add health check / ping on startup

### 1.4 Redis connection (`internal/redis/`)
- [x] Implement `Connect(ctx, redisURL) (*redis.Client, error)`
- [x] Add ping on startup

### 1.5 Migrations (`migrations/`)
- [x] `000001_create_users.up.sql` — create `users` table
- [x] `000001_create_users.down.sql` — drop `users` table
- [x] `000002_create_routes.up.sql` — create `routes` table with CHECK constraints
- [x] `000002_create_routes.down.sql` — drop `routes` table

### 1.6 Entrypoint (`cmd/bot/main.go`)
- [x] Load config
- [x] Initialize slog JSON logger
- [x] Connect to PostgreSQL and Redis
- [x] Run migrations programmatically on startup
- [x] Graceful shutdown via `signal.NotifyContext`

### 1.7 Dockerfile
- [x] Multi-stage Alpine build
- [x] Docker Compose with health checks for postgres and redis

---

## Phase 2 — Domain & Data Layer (DONE)

> Goal: domain types, interfaces, sqlc queries, and generated Go code.
> Note: `User` and `Route` structs are generated by sqlc from the migration schema — do not duplicate them in `domain/`.

### 2.1 Domain types (`internal/domain/`)
- [x] `TrainStatus` struct in `train.go` — ServiceID, Destination, ScheduledDeparture, EstimatedDeparture, Platform, IsCancelled, DelayMins
- [x] `NearestTrains` struct in `train.go` — Exact, Before, After (all `*TrainStatus`)
- [x] User state enum in `user.go` — `StateNew`, `StateAwaitingFrom`, `StateAwaitingTo`, `StateAwaitingTime`, `StateAwaitingTrainChoice`, `StateAwaitingDays`, `StateAwaitingLabel`, `StateAwaitingDelete`, `StateReady`
- [x] `IsValidState()` helper

### 2.2 Interfaces (`internal/domain/interfaces.go`)
- [x] `TrainClient` interface — `GetDepartureBoard(ctx, fromCRS, toCRS, timeOffsetMins) ([]TrainStatus, error)`, `GetServiceDetails(ctx, serviceID) (*TrainStatus, error)`
- [x] `Notifier` interface — `Send(ctx, chatID int64, message string) error`, `Broadcast(ctx, chatIDs []int64, message string) error`, `SendTrainChoice(...)`, `SendBetterTrainOffer(...)`

### 2.3 sqlc queries (`queries/`)
- [x] `user.sql` — CreateUser (upsert), GetUserByChatID, UpdateUserState
- [x] `route.sql` — CreateRoute, GetRoutesByUserID, GetActiveRoutesForWeekday (bitmask), UpdateRouteActive, DeleteRoute, CountRoutesByUserID
- [x] `sqlc generate` produces `internal/db/` — models.go, db.go, user.sql.go, route.sql.go
- [x] `connect.go` added separately for DB pool (not overwritten by sqlc)

### 2.4 Station codes (`internal/station/`)
- [x] `internal/station/data/stations.json` — embedded UK CRS codes
- [x] `Lookup(crs) (name, bool)` and `IsValid(crs) bool` with `sync.Once` lazy loading

---

## Phase 3 — Telegram Bot (DONE)

> Goal: working bot with full conversation state machine for route setup.

### 3.1 Bot setup (`internal/bot/bot.go`)
- [x] Initialize telebot v4 with long polling
- [x] Register command handlers
- [x] Accept `db.Queries` and `redis.Client` dependencies
- [x] Implement `Notifier` interface — `Send()` and `Broadcast()` via telebot

### 3.2 Command handlers (`internal/bot/handlers.go`)
- [x] `/start` — register user (upsert via DO NOTHING), send welcome message
- [x] `/status` — show user's active routes with station names, times, days, status
- [x] `/stop` — deactivate all active routes, confirm count, hint `/resume`
- [x] `/resume` — reactivate all paused routes, confirm count
- [x] `/add` — begin route creation flow (enforces max 2 routes, clears draft, sets `StateAwaitingFrom`)
- [x] `/delete` — single route: delete immediately; multiple: prompt for choice via inline buttons and `StateAwaitingDelete`
- [x] `/help` — show available commands, how the bot works, and usage tips

### 3.3 Conversation state machine (text message handler)
- [x] `StateAwaitingFrom` — fuzzy station search (name or CRS code), save to Redis draft, transition to `StateAwaitingTo`
- [x] `StateAwaitingTo` — fuzzy station search, reject same-as-from, save, transition to `StateAwaitingTime`
- [x] `StateAwaitingTime` — HH:MM validation with time normalization (e.g. "8:30" → "08:30"), queries Darwin for nearest trains if within range, transition to `StateAwaitingTrainChoice` (if no exact match) or `StateAwaitingDays`
- [x] `StateAwaitingTrainChoice` — inline buttons for selecting between nearest before/after trains, or enter new time
- [x] `StateAwaitingDays` — parse day names (Mon-Sun), groups (`weekdays`, `weekends`, `all`), save bitmask, transition to `StateAwaitingAlerts`
- [x] `StateAwaitingAlerts` — parse comma-separated minutes (1-180 range, max 3), validate, dedup+sort desc, save to draft, transition to `StateAwaitingLabel`
- [x] `StateAwaitingLabel` — validate 1-50 chars, create route in DB with user-configured alert offsets, clear draft, set `StateReady`, immediately plan route and show train status
- [x] `StateAwaitingDelete` — parse 1/2 choice via inline buttons or text, delete selected route, set `StateReady`
- [x] Enforce max 2 routes per user
- [x] Invalid input at each state re-prompts with helpful message
- [x] Route drafts stored in Redis hash with 30-min TTL (`internal/bot/draft.go`) — fields: from, to, time, days, alerts, train_option_1, train_option_2

### 3.4 Tests
- [x] `isValidTimeFormat` — table-driven (valid, invalid, edge cases)
- [x] `formatTime` — pgtype.Time to string conversion
- [x] `draftKey` — Redis key format

### 3.5 Wiring
- [x] `cmd/bot/main.go` wires up `db.Queries` → `bot.New()` → `tgBot.Start()` in goroutine
- [x] Graceful shutdown calls `tgBot.Stop()`
- [x] Added `StateAwaitingDelete`, `StateAwaitingTrainChoice`, `StateAwaitingDays` to domain user states
- [x] Bot receives `RoutePlanner` interface for immediate route planning and train search
- [x] Inline callback handlers registered for `\fdelete` and `\ftrain`

---

## Phase 4 — Darwin SOAP Client (DONE)

> Goal: working National Rail API integration.

### 4.1 SOAP client (`internal/darwin/client.go`)
- [x] Implements `TrainClient` interface
- [x] `GetDepartureBoard` — SOAP `GetDepBoardWithDetails` with optional `filterCrs` for destination filtering
- [x] `GetServiceDetails` — SOAP `GetServiceDetails` by service ID
- [x] SOAP envelope marshaling via `fmt.Sprintf` templates, XML unmarshaling via `encoding/xml`
- [x] 10s HTTP timeout, configurable endpoint (for testing)
- [x] Maps SOAP fields (std, etd, platform, isCancelled) to `domain.TrainStatus` with delay calculation

### 4.2 Error handling (`internal/darwin/errors.go`)
- [x] `TransientError` — timeouts, 500/503, read failures (circuit breaker should retry)
- [x] `PermanentError` — 400/401 (should not retry)
- [x] `IsTransient(err)` helper for circuit breaker integration

### 4.3 Tests (`internal/darwin/client_test.go`)
- [x] `GetDepartureBoard` — on time, multiple services, empty board, server error, unauthorized
- [x] `GetServiceDetails` — on time, delayed (7 min), cancelled, no platform
- [x] Error classification — 500 returns `TransientError`
- [x] `parseTrainTime` — valid times, invalid input, empty string

---

## Phase 5 — Daily Planner (DONE)

> Goal: cron job that resolves today's service IDs and caches them.

### 5.1 Planner (`internal/tracker/planner.go`)
- [x] Fetch active routes for today's weekday via bitmask query
- [x] For each route, call `TrainClient.GetDepartureBoard(from, to, timeOffset)` and find closest service within 5-min tolerance
- [x] Store result in Redis: key = `route_service:{routeID}`, value = JSON-serialized `TrainStatus`
- [x] Set Redis TTL to end of day
- [x] Log summary: total routes, planned, failed

### 5.2 Scheduling & API
- [x] `PlanRoute(ctx, route)` — exported for use by bot and daemon
- [x] `FindNearestTrains(ctx, fromCRS, toCRS, targetMins)` — returns Exact, Before, and After trains for interactive selection
- [x] `departureOffset()` — calculates API time offset based on UK time, clamped to ±120 min

### 5.3 Tests
- [x] `findClosestService` — exact match, closest early/late, equidistant, out of tolerance, empty
- [x] `pgTimeToMinutes` — morning, midnight, evening, invalid
- [x] `todayWeekdayBit` — matches current weekday
- [x] `serviceCacheKey` / `formatUUID` — key format, invalid UUID
- [x] `absDiff` / `timeUntilEndOfDay` — edge cases

### 5.4 Wiring
- [x] `cmd/bot/main.go` creates `darwin.NewClient` + `tracker.NewPlanner`, calls `planner.Start(ctx)`

---

## Phase 6 — Tracker Daemon (DONE)

> Goal: polling loop that detects state changes and pushes alerts.

### 6.1 Daemon (`internal/tracker/daemon.go`)
- [x] Ticks every 2 min, fetches active routes with chat IDs via `GetActiveRoutesWithChatID` join query
- [x] Filters routes by alert window — iterates each offset in `alert_offsets[]`, checks `departure_time - offset <= now <= departure_time`
- [x] Clears cache for departed routes
- [x] Gets cached service from planner, fetches fresh status via `TrainClient.GetServiceDetails`
- [x] Compares with `LastKnownState` in Redis — notifies only on change
- [x] First check (no last state): sends initial status alert
- [x] Saves `LastKnownState` in Redis with end-of-day TTL

### 6.2 Notification formatting
- [x] Initial alert: route label, from/to with names, departure time, platform (or TBC), status
- [x] Change alerts: specific diffs — "Platform changed: 3 -> 7", "Delayed 7 min", "CANCELLED", "Now on time"

### 6.3 Tests
- [x] `statusChanged` — no change, platform change, cancellation, delay, estimated time change
- [x] `isWithinAlertWindow` — before, start, middle, at departure, after
- [x] `isDeparted` — before, at, after departure
- [x] `formatAlert` — initial (on time, no platform), delayed, cancelled, platform change
- [x] `orTBC` — empty and non-empty

### 6.4 Data layer
- [x] Added `GetActiveRoutesWithChatID` sqlc query (routes JOIN users)
- [x] Regenerated sqlc code

### 6.5 Wiring
- [x] `cmd/bot/main.go` creates `tracker.NewDaemon(queries, trainClient, tgBot, rdb)`, `tgBot` serves as `Notifier`

---

## Phase 7 — Circuit Breaker (DONE)

> Goal: graceful API downtime handling with user notifications.

### 7.1 Circuit breaker (`internal/tracker/circuit.go`)
- [x] In-memory consecutive failure counter with mutex synchronization
- [x] After 3 failures: transition to `APIDown`, broadcast warning to active users
- [x] Spawns health check goroutine: pings Darwin every 5 min with `GetDepartureBoard("KGX", "")`
- [x] On successful health check: transition to `APIUp`, broadcast recovery, stop health check loop
- [x] Double-transition guard: ignores redundant `transitionToDown` calls

### 7.2 Integration with daemon
- [x] Daemon checks `circuitBreaker.State()` at start of each tick — skips if `APIDown`
- [x] `checkRoute` calls `RecordFailure` on Darwin errors, `RecordSuccess` on success
- [x] Counter resets on any success during `APIUp`

### 7.3 Data layer
- [x] Added `GetActiveChatIDsForWeekday` sqlc query (DISTINCT chat IDs via routes JOIN users)
- [x] `getChatIDs` callback passed from `main.go` to circuit breaker

### 7.4 Tests
- [x] Starts in `APIUp` state
- [x] Trips to `APIDown` after 3 consecutive failures, broadcasts to users
- [x] Counter resets on success — doesn't trip after reset + 1 failure
- [x] Double transition sends only 1 broadcast
- [x] `healthPing` success and failure
- [x] Mock `TrainClient` and `Notifier` for isolated testing

### 7.5 Wiring
- [x] `cmd/bot/main.go` creates `CircuitBreaker` with `getChatIDs` callback, passes to `NewDaemon`
- [x] Exported `TodayWeekdayBit()` for use in main

---

## Phase 8 — Integration & Hardening (DONE)

> Goal: wire everything together, end-to-end testing, production readiness.

### 8.1 Wire up `main.go`
- [x] All components wired: config → logger → DB → Redis → Darwin → bot → planner → circuit breaker → daemon
- [x] Bot, planner, daemon run as concurrent goroutines
- [x] Graceful shutdown: signal → cancel ctx → stop bot → defer close DB/Redis pools

### 8.2 Logging
- [x] Structured slog JSON throughout with `route_id`, `service_id`, `chat_id`, `error` fields
- [x] INFO: normal ops, WARN: transient API failures, ERROR: failures, DEBUG: tick summaries

### 8.3 Docker
- [x] Multi-stage Dockerfile (golang:1.26-alpine → alpine:3.21)
- [x] Docker Compose with health checks, named volumes (postgres:18, redis:8-alpine)
- [x] `.dockerignore` excludes `.git`, `.env`, IDE files

### 8.4 Lint & Code Quality
- [x] `golangci-lint run` passes with 0 issues
- [x] Fixed `.golangci.yml` for v2 (formatters/linters separation, removed deprecated `gosimple`)
- [x] All `errcheck`, `errorlint`, `staticcheck` issues resolved

### 8.5 Final checks
- [x] `go test -race` passes across all packages
- [x] Coverage: domain 100%, darwin 91%, station 86%, tracker 80%, bot 72% (bot.go infrastructure untestable without Telegram token)
- [x] No secrets in committed files (`.env` gitignored)
- [x] `.env.example` documents all required keys

---

## Phase 9 — Enhanced UX & Train Discovery (DONE)

> Goal: smarter route creation with real-time train lookup, days-of-week selection, and inline button UI.

### 9.1 Station fuzzy search (`internal/station/`)
- [x] `Search(query)` — fuzzy matching against embedded station list using `sahilm/fuzzy`
- [x] Returns up to 5 results with CRS + name
- [x] `resolveStation()` in handlers — resolves CRS directly or via single fuzzy match, prompts if ambiguous

### 9.2 Train discovery during route creation
- [x] `Planner.FindNearestTrains(ctx, fromCRS, toCRS, targetMins)` — returns exact match or closest before/after
- [x] `NearestTrains` domain type with Exact, Before, After fields
- [x] `isWithinDarwinRange()` — only queries API when target is within ±120 min of now
- [x] `formatTrainOptions()` — builds message with before/after suggestions and button labels

### 9.3 Inline button UI
- [x] Train selection: inline buttons for choosing between nearest trains (`\ftrain` callback)
- [x] Route deletion: inline buttons when user has multiple routes (`\fdelete` callback)
- [x] `handleTrainCallback` and `handleDeleteCallback` handlers in bot

### 9.4 Days-of-week selection
- [x] New `StateAwaitingDays` state in conversation flow (after time, before label)
- [x] `parseDaysMask()` — supports individual days (Mon-Sun), groups (`weekdays`, `weekends`, `all`), and combinations
- [x] `formatDaysMask()` — renders bitmask as readable text (e.g. "Mon-Fri", "Mon, Wed, Fri")
- [x] Bitmask stored in route draft and persisted to `days_of_week` column

### 9.5 Immediate route planning
- [x] `planRouteNow()` — after route creation, immediately attempts to find and cache the train service
- [x] Shows live status (platform, on-time/delayed) in creation confirmation message
- [x] Falls back to nearest-train suggestions if exact match not found
- [x] `RoutePlanner` interface in bot package for dependency injection

### 9.6 Misc improvements
- [x] `normalizeTime()` — accepts "8:30" and normalizes to "08:30"
- [x] Alert offsets changed from single `alert_offset_mins` integer to `alert_offsets` integer array (user-configurable, 1-3 values, 1-180 min range)
- [x] `TrainStatus.Destination` field added — displayed in departure reminders and train selection
- [x] `departureOffset()` uses Europe/London timezone for correct offset calculation
- [x] Match tolerance tightened from 30 to 5 minutes

---

## Phase 10 — Better Train Detection (DONE)

> Goal: when tracking a non-ideal train (outside ±5 min of desired time), detect if a closer train has appeared and offer the user a one-tap switch.

### 10.1 Planner extension (`internal/tracker/planner.go`)
- [x] `FindBetterTrain(ctx, fromCRS, toCRS, targetMins, currentServiceID)` — fetches departure board, returns a closer match (within tolerance) if different from current service

### 10.2 Daemon logic (`internal/tracker/daemon.go`)
- [x] `checkBetterTrain()` — called after `checkRoute()` in the tick loop
- [x] Short-circuits early if cached train is within ±5 min tolerance (no API call)
- [x] Short-circuits if `better_offered` or `better_declined` Redis flag is set (no API call)
- [x] Only calls `GetDepartureBoard` when all conditions pass — one extra API call per tick for non-ideal routes
- [x] On finding a better train: sends offer via `SendBetterTrainOffer`, sets `better_offered` flag
- [x] Redis keys `better_declined:` and `better_offered:` with end-of-day TTL (auto-expire overnight, ask again next day)
- [x] `clearRouteCache` updated to clear both new keys

### 10.3 Notifier interface (`internal/domain/interfaces.go`)
- [x] Added `SendBetterTrainOffer(ctx, chatID, routeID, currentTime, betterTime, betterDestination)` to `Notifier` interface

### 10.4 Bot implementation (`internal/bot/`)
- [x] `SendBetterTrainOffer` — builds inline keyboard message with Switch / Keep buttons
- [x] `handleBetterSwitchCallback` — validates ownership, calls `PlanRoute` with better time, clears `better_offered`; graceful fallback if train disappeared
- [x] `handleBetterKeepCallback` — sets `better_declined` flag for rest of day, clears `better_offered`
- [x] Registered `\fbetterswitch` and `\fbetterkeep` callback handlers

### 10.5 Tests (`internal/tracker/better_train_test.go`)
- [x] Cached within tolerance — no API call (table-driven with call-counting mock)
- [x] Already declined / already offered — no API call
- [x] Better train found — offer sent, flag set, correct times and destination
- [x] No better available — no offer, no flag
- [x] Same service returned — no offer
- [x] API failure — no offer, circuit breaker records failure
- [x] Declined expires next day (simulated key deletion)
- [x] `clearRouteCache` clears both new Redis keys
- [x] `FindBetterTrain` — found, same service, none in tolerance, API error, empty board
- [x] Mark/check helpers for both flags
- [x] Integration: `checkRoute` + `checkBetterTrain` together — no false status alerts
- [x] Offer not repeated after first send (3 consecutive calls = 1 offer)

---

## Phase 11 — CI Pipeline (DONE)

> Goal: GitHub Actions CI that gates all pull requests to `main`.

### 11.1 Workflow (`.github/workflows/pr-check.yml`)
- [x] Trigger on `pull_request` to `main`
- [x] **Format** job — `gofmt -l .` fails on unformatted files
- [x] **Vet** job — `go vet ./...` static analysis
- [x] **Lint** job — `golangci-lint` via official action
- [x] **SQLc** job — `sqlc diff` verifies generated code is up to date
- [x] **Test** job — `go test -race -count=1 ./...`
- [x] **Build** job — compiles binary to verify successful build
- [x] All 6 jobs run in parallel for fast feedback

---

## Phase 12 — Production Hardening (Hetzner Release) (DONE)

> Goal: make the bot secure, resilient, and operationally sound for running on a Hetzner Cloud VPS.

### 12.1 Docker Compose — Network & Secrets

- [x] Remove exposed ports for postgres and redis — bot uses Docker internal network, no need to expose DB/cache to the internet
- [x] Extract hardcoded credentials (`POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`) into `.env` via `${VAR}` substitution
- [x] Extract `DATABASE_URL` and `REDIS_URL` into `.env` with Docker-internal defaults
- [x] Add log rotation to all services (`json-file` driver, `max-size: 10m`, `max-file: 3`)
- [x] Update `.env.example` with the new variables

### 12.2 Dockerfile — Security & Optimization

- [x] Create non-privileged user (`app:app`) in runtime stage, add `USER app` before `ENTRYPOINT`
- [x] Add `-ldflags="-s -w"` to `go build` — strip debug symbols, reduce binary size
- [x] Add `-trimpath` to `go build` — remove local filesystem paths from stack traces

### 12.3 Graceful Shutdown (`cmd/bot/main.go`)

- [x] Add `sync.WaitGroup` to track daemon goroutine lifecycle
- [x] Wait for daemon to complete in-flight tick before exiting after `ctx.Done()`
- [x] Add 10s shutdown timeout to prevent hanging forever on exit

### 12.4 Context Propagation (`internal/bot/handlers.go`)

- [x] Replace all `context.Background()` calls in handlers with `context.WithTimeout` (10s)
- [x] Telebot v4 doesn't expose Go `context.Context` on its Context interface, so handlers use `context.Background()` with explicit timeout

### 12.5 Connection Pool Tuning

- [x] **PostgreSQL** (`internal/db/connect.go`) — `pgxpool.ParseConfig()` with `MaxConns=10`, `MinConns=2`, `MaxConnIdleTime=5m`, `MaxConnLifetime=30m`, `HealthCheckPeriod=30s`
- [x] **Redis** (`internal/redis/redis.go`) — `MaxRetries=3`, `PoolSize=10`, `MinIdleConns=2`

### 12.6 Panic Recovery

- [x] Add `defer recover()` with `slog.Error` in daemon goroutine (`internal/tracker/daemon.go`)
- [x] Add `defer recover()` with `slog.Error` in circuit breaker health check goroutine (`internal/tracker/circuit.go`)

### 12.7 Error Handling Improvements

- [x] **Broadcast** (`internal/bot/bot.go`) — collect all errors with `errors.Join()` instead of returning only the last one
- [x] **Station init** (`internal/station/station.go`) — return error via `Init()` instead of panic; called at startup in `main.go`

### 12.8 Rate Limiting

- [x] Add per-user command rate limiting via Redis (max 20 per minute) in `internal/bot/ratelimit.go`
- [x] Implemented as telebot middleware applied to all handlers via `b.Use()`

### 12.9 Configuration

- [x] Make daemon tick interval configurable via `DAEMON_TICK_INTERVAL` env var (default `2m`)
- [x] Add config validation for `LOG_LEVEL` (accept only debug/info/warn/error)

### 12.10 Tests

- [x] All existing tests pass with changes (`go test -race ./...`)
- [x] `golangci-lint run ./...` passes with 0 issues

---

## Phase 13 — Optional System Alerts (DONE)

> Goal: make circuit breaker notifications opt-in per user, with better emoji formatting.

### 13.1 Database
- [x] Migration `000004_system_alerts` — add `system_alerts BOOLEAN NOT NULL DEFAULT false` to `users`
- [x] sqlc queries: `ToggleSystemAlerts` (toggle + return new value), `GetSystemAlertsChatIDs` (users with alerts enabled)

### 13.2 Bot
- [x] `/alerts` command — toggles `system_alerts` for the user
- [x] Updated help text with `/alerts` command

### 13.3 Circuit Breaker
- [x] Changed notification format: `⚠️` for API down, `✅` for recovery (removed `[System]` prefix)
- [x] Broadcasts only to users with `system_alerts = true` (via `GetSystemAlertsChatIDs`)
- [x] Alerts disabled by default for all users
